import { TimeAgoKorPipe } from './time-ago-kor.pipe';

describe('TimeAgoKorPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAgoKorPipe();
    expect(pipe).toBeTruthy();
  });
});
